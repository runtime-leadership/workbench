# Skills

Skills are reusable instructions for completing a repeatable workflow with an AI tool.

Canonical skills should remain readable as ordinary files. Platform-specific adapters may point to them when OpenCode, Claude Code, Codex, Cursor, or another tool requires a special discovery path.

Existing skills are maintained upstream. Add new files for local skills instead of placing private context in canonical files.
