# Agents

Agents are delegated roles with a defined job, relevant context, and clear boundaries.

An agent may use templates, resources, skills, knowledge, or project files, but it should not become a duplicate source of truth for those materials.

Existing agents are maintained upstream. Add new files for local agents instead of placing private context in canonical files.
